# Copie para .env e preencha. NUNCA versione o .env real.

# Opcao 1: PostgreSQL direto (mais rapido, requer porta 5432 aberta)
# Supabase > Settings > Database > Connection string > URI
DATABASE_URL=

# Opcao 2: API REST do Supabase (HTTPS/443, passa por firewalls corporativos)
# Supabase > Project Settings > API > URL e anon/public key
SUPABASE_URL=https://jcmxxdbheuymophxyysk.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIs...  # anon/public key

# Se ambas vazias, o sistema usa dados sinteticos/CSV locais (fallback).
